spring:
  datasource:
    url: jdbc:mysql://localhost:3306/javaWeb?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: off
    properties:
      hibernate:
        format_sql: true
server:
  port: 8081
mybatis:
  configuration:
    map-underscore-to-camel-case: true